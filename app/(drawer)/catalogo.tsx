import { CatalogoScreen } from "../../screen/catalogo/catalogo";

/**
 * Entry point para el tab/drawer de Catálogo.
 * Mantiene la navegación limpia delegando la UI a la carpeta screen.
 */
export default function Page() {
  return <CatalogoScreen />;
}
